/** Set apiUrl to your deployed Node API (HTTPS). CORS on the API must allow your Firebase Hosting origin. */
export const environment = {
  production: true,
  apiUrl: 'https://your-api.example.com',
};
